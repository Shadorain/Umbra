use umbra;

/// Will be a simple example of rendering a Document with Umbra
fn main() {
    println!("Hello, world!");
    // let mut umbra = Umbra::new().config(serialized_data);
    //
    // IDEA: umbra.with_... builder pattern config
    // 
}
